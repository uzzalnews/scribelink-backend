# Choose one provider: "openai" or "assemblyai"
TRANSCRIBE_PROVIDER=openai

# Required if TRANSCRIBE_PROVIDER=openai
OPENAI_API_KEY=sk-...

# Required if TRANSCRIBE_PROVIDER=assemblyai
ASSEMBLYAI_API_KEY=...

PORT=3000
